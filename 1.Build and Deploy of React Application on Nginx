# DevOps-Project

Build and Deploy Series | How to Deploy React Application in Nginx and AWS


AWS2-  NGINX- REACT



- click on launch instance
- name- reast-nginx-aws
- ubuntu-os
- keypair- react-nginx-aws-deploy
- T3 micro
- allow ssh port 22 for ipv4/ipv6 0.0.0.0/0
- allow custom tcp port 80 for nginx 0.0.0./0


- ssh -i react-nginx-aws-deploy ubuntu@publicipadress of reast-nginx-aws

    1  sudo apt install git
    2  git clone https://github.com/akhilgit19/Gold_Site_Ecommerce-1.git
    3  ls
    4  sudo apt update
    5  sudo apt install nodejs
    6  node --version
    7  sudo apt install npm
    8  npm --version
    9  ls
   10  cd Gold_Site_Ecommerce-1
   11  ls
   12  npm install react-scripts
   13  ls -lhtr
   14  npm run build
   15  ls -lhtr
   16  sudo apt install nginx
   17  nginx -version
   18  ls
   19  cd /home/ubuntu/Gold_Site_Ecommerce-1
   20  sudo vi /etc/nginx/sites-available/default
       add build in root /var/www/html;
       as
       root /var/www/html/build;
       
       you can provide server name as your goddayd account server ip
    server_name _;
     as
     server_name hari_dev.shop.
       sudo su - ubuntu
   21  sudo cp -R build/ /var/www/html/
   22  sudo systemctl restart nginx
   23  sudo systemctl status nginx
   24. Log into your GoDaddy Console and My Products >Domain > DNS.In A records, paste the AWS EC2 public IP
       add the ip in the record value 
       type A
       Name @
       value 13.49.67.11, 
       TTL custom, 
        seconds -60



 

